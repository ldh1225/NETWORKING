package com.example.notification;

import java.util.List;

import org.springframework.stereotype.Service;

@Service
public class NotificationService {

    // 예제 메서드, 실제 로직으로 교체해야 합니다.
    public void sendLikeNotification(Notification notification) {
        // 알림을 처리하는 로직을 여기에 추가합니다.
    }

    public List<Notification> fetchNotifications(String userId) {
        // 사용자 ID에 따른 알림을 가져오는 로직을 여기에 추가합니다.
        return null; // 실제 로직에 따라 반환 값을 교체합니다.
    }
}
